[package]
name = "vec_mem_heap"
version = "0.7.0"
edition = "2024"
description = "An arena allocator with reference counting."
repository = "https://github.com/Karanstr/vec_mem_heap"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
